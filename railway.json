{ {
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd api-server && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd api-server && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}

