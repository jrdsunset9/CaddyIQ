node_modules/
dist/
.env
.env.local
.env*.local
api-server/node_modules/
api-server/dist/

# Build artifacts / large bundles — never commit
*.zip
Caddy-Iq.zip
caddyiq.zip

# Claude Code internal settings — not needed in repo
.claude/
